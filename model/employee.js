class Employee {
  constructor(id, name, designation, level, status, salary, score) {
    (this.id = id),
      (this.name = name),
      (this.designation = designation),
      (this.level = level),
      (this.status = status),
      (this.salary = salary),
      (this.score = score);
  }
}

export default Employee;
